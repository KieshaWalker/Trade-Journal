{
    "version": 2,
    "builds": [
        {
            "src": "api/asgi.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/static/(.*)",
            "dest": "/static/$1"
        },
        {
            "src": "/(.*)",
            "dest": "api/asgi.py"
        },
        {
            "src": "/api/(.*)",
            "dest": "api/asgi.py"
        }
    ]
}